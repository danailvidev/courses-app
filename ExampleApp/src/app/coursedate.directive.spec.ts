import { CoursedateDirective } from './coursedate.directive';

describe('CoursedateDirective', () => {
  it('should create an instance', () => {
    const directive = new CoursedateDirective();
    expect(directive).toBeTruthy();
  });
});
